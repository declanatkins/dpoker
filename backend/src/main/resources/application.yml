spring:
  datasource:
    url: jdbc:h2:mem:pokerdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  secret: ${JWT_SECRET}
  access_expiration: 1800
  refresh_expiration: 86400
